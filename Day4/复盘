1. 字符串索引为什么从0开始？
底层原因,Python 和很多编程语言一样使用 0-based indexing，第一个元素索引是 0。
2. text[0:3] 为什么只得到3个字符？
因为只切了3个,另外包含索引 0,text[0:3]包含索引 1、包含索引 2、不包含索引 3
3. strip() 可以解决什么真实数据问题？
脏数据清洗
4. split() 的返回结果是什么？
列表
5. 普通变量和列表最大的区别是什么？
普通变量可被覆盖，而列表能够存多个变量
6. append() 做了什么？
添加元素到列表后面
7. account 和 accounts 在循环里分别承担什么角色？
account是承担输入的角色，而accounts是保存输入的数据
8. 为什么先把原始数据保存进列表，再做统计，通常比只保留 total 更有价值？
如果有数据错误可以进行修改，当有多个数据时候，若需要庞大的计算，而由于部分错误而改全局就显得很不合理;保留原始列表之后，不仅能“改错”，还能重新计算平均值、最大值、最小值、筛选异常数据、重新分类，而只有 total 时这些信息已经丢失了。

主要学了列表，列表的 strip() 脏数据处理、split() 切割 并返回列表、text[] 字符串索引 、 text[0:3] 切片  、