一、字符串 str
字符串就是一串文本。在 Python 中通常用引号表示：
name = "银行存款"
date = "2026-09-15"
此时：
print(type(name))
会得到：
<class 'str'>
1. 字符串索引
字符串里的每个字符都有自己的位置，Python 从 0 开始编号。
account = "银行存款"

print(account[0])
print(account[1])
print(account[2])

结果：
银
行
存
可以理解成：
字符：  银  行  存  款
索引：  0   1   2   3
所以：
account[0]
表示“取出第一个字符”。

还可以从后往前：

字符：  银  行  存  款
负数： -4  -3  -2  -1

所以：

print(account[-1])

得到：

款
2. 字符串切片

索引是“拿一个”，切片就是“一次拿一段”。

text = "2026-09-15"

print(text[0:4])

得到：

2026

这里：

text[0:4]

可以理解为：

从索引 0 开始，一直取到索引 4 之前。

也就是：

0 1 2 3
2 0 2 6

注意一个非常重要的规则：

左边包含，右边不包含。

所以：

print(text[5:7])

得到：

09
3. len()：计算长度
account = "银行存款"

print(len(account))

结果：

4

也就是字符串有 4 个字符。

以后可以用于判断：

if len(account) == 0:
    print("科目名称不能为空")
4. strip()：去掉两边多余空白

这个对以后处理 Excel、用户输入和脏数据非常有用。

例如：

account = "   银行存款   "

print(account)

实际上两边有很多空格。

使用：

account = account.strip()

print(account)

就会变成：

银行存款

所以以后用户输入：

account = input("请输入科目名称：").strip()

就可以顺手清理两侧空格。

这就是为什么：

" 银行存款 ".strip()

对于数据清洗有实际意义。

5. lower() 和 upper()

主要处理英文字母大小写。

text = "Python"

print(text.lower())
print(text.upper())

结果：

python
PYTHON

例如以后判断用户输入：

answer = input("是否继续？yes/no：").strip().lower()

if answer == "yes":
    print("继续")

用户输入：

YES
Yes
yes

经过 .lower() 后都会变成：

yes
6. replace()：替换内容
text = "2026/09/15"

new_text = text.replace("/", "-")

print(new_text)

结果：

2026-09-15

它的基本结构就是：

字符串.replace("旧内容", "新内容")

以后做数据清洗非常常见。

7. split()：拆分字符串

例如：

date = "2026-09-15"

result = date.split("-")

print(result)

结果：

['2026', '09', '15']

注意这里发生了一个非常重要的变化：

原来：
"2026-09-15"
      ↓
    字符串

split("-")
      ↓

['2026', '09', '15']
      ↓
     列表

这正好就可以引出今天第二个重点：列表 list。

二、列表 list

前三天你可能经常这样保存数据：

account = "银行存款"

问题是：

account = "银行存款"
account = "库存现金"
account = "应收账款"

最后 account 里面只剩：

应收账款

因为前面的值被覆盖了。

但如果我希望：

一次保存很多个科目怎么办？

这时候就需要列表。

1. 创建列表
accounts = ["银行存款", "库存现金", "应收账款"]

print(accounts)

结果：

['银行存款', '库存现金', '应收账款']

一个变量现在可以保存多个数据。

也可以先创建空列表：

accounts = []
2. 列表也有索引
accounts = ["银行存款", "库存现金", "应收账款"]

print(accounts[0])

得到：

银行存款

对应：

元素：银行存款  库存现金  应收账款
索引：   0        1        2

这和刚才字符串索引的思想是一样的。

3. append()：往列表后面添加数据
accounts = []

accounts.append("银行存款")
accounts.append("库存现金")
accounts.append("应收账款")

print(accounts)

得到：

['银行存款', '库存现金', '应收账款']

注意：

accounts.append("银行存款")

不是覆盖，而是：

把“银行存款”追加到 accounts 这个列表后面。

这就是今天非常重要的知识点。

4. 修改列表元素
accounts = ["银行存款", "库存现金", "应收账款"]

accounts[1] = "现金"

print(accounts)

得到：

['银行存款', '现金', '应收账款']

因为：

accounts[1]

就是第二个元素。

5. len() 也可以计算列表长度
accounts = ["银行存款", "库存现金", "应收账款"]

print(len(accounts))

得到：

3

所以：

len("银行存款")

表示：

字符串有几个字符？

而：

len(accounts)

表示：

列表有几个元素？

6. for 遍历列表

你 Day 3 已经学了 for，现在开始把它和列表组合。

accounts = ["银行存款", "库存现金", "应收账款"]

for account in accounts:
    print(account)

结果：

银行存款
库存现金
应收账款

这里可以理解成：

第一次循环
account = "银行存款"

第二次循环
account = "库存现金"

第三次循环
account = "应收账款"

所以你会发现：之前学的知识开始连接起来了。



先掌握字符串的基础操作：索引、切片、len()、strip()、lower()/upper()、replace() 和 split()。
重点不是背方法，而是能理解为什么 " 银行存款 ".strip() 可以用于清理脏数据，以及 "2026-09-15".split("-") 为什么能够拆出年月日。
然后进入今天真正的重点——列表 list。
需要掌握列表创建、索引、append()、修改元素、len() 和使用 for 遍历列表。今天暂时不要追求把所有列表方法学完。

先自己做一个很小的实验：

accounts = []

for i in range(3):
    account = input("请输入科目名称：")
    accounts.append(account)

print(accounts)

运行后重点观察：为什么循环已经结束了，前三次输入的科目名称却仍然能够一起输出？这就是列表与前三天“普通变量”的关键区别。